<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <%= paginate @photos %>
  </div>
</div>

<div id="photos">
  <div class="page">
  <% @photos.each do |photo| %>
    <%= render 'photo', :photo => photo %>
  <% end %>
  </div>
</div>




